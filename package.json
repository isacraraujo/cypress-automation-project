{
  "devDependencies": {
    "cypress": "^14.5.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-plugin-api": "^2.11.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.8.0"
  },
  "scripts": {
    "cy:installer": "npm install -D cypress && npm install -D cypress-mochawesome-reporter && npm install -D @faker-js/faker && npm i -D cypress-plugin-api",
    "cy:run_chrome": "npx cypress run --browser chrome",
    "report": "start cypress/reports/report.html",
    "cypress": "npm run cy:run_chrome && npm run report"
  }
}
